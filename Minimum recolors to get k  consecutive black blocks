class Solution(object):
    def minimumRecolors(self, blocks, k):
        """
        :type blocks: str
        :type k: int
        :rtype: int
        """
        min_=float('inf')

        for i in range(len(blocks)-k+1):
            min_=min(blocks[i:i+k].count('W'),min_)

        return min_
